Email Generator Exercise
This project demonstrates how to generate random emails using the Axios library and display them in an HTML list. The script fetches random email addresses from an API and dynamically updates the webpage.

Features
Generates random email addresses using an external API.
Displays the emails in a list on the webpage.
Utilizes JavaScript, Axios, and DOM manipulation.
